{
  "version": "ng1.0.2",
  "north_star_v52_score": "74.0% A+ (CPPI后)",
  "date": "2026-04-05",
  "description": "NG1.0.2: 69特征 + 行业超额标签 + 下行风险模型 + ICIR自适应 + CPPI(5,20)",

  "model": {
    "scoring_version": "ng1.0.2",
    "model_path": "ml_models/trained_models/ng/ng_multi_target_20260405_194751.pkl",
    "features": 69,
    "rank_field": "composite",
    "cache_table": "ng102_feature_cache",
    "downside_model": true,
    "lambda_risk": 0.5
  },

  "portfolio": {
    "top_n": 10,
    "focus_days": 10,
    "cppi_floor": 0.05,
    "cppi_multiplier": 20,
    "_cppi_explanation": "净值接近floor时自动减仓: exposure = min(1.0, 20 × (净值 - floor净值))"
  },

  "recommendation_ranks": {
    "_calibration": "2024-01-01~2026-04-03, 543天, ng1.0.2模型, 基于截面排名的alpha验证",
    "_evidence": "Top1-5 10d超额+3.4%, 胜率66%; 信号强度与alpha相关系数=0.022(无关); 强熊时Top5超额反而最高(+8.7%)",
    "strong_buy": 5,
    "buy": 20,
    "cautious_buy": 50,
    "watch": 200,
    "_strong_buy_10d_excess": "+3.4% (年化~250%)",
    "_buy_10d_excess": "+1.7% (年化~135%)",
    "_cautious_10d_excess": "+0.8% (年化~89%)"
  },

  "cppi_usage": {
    "_how_to_use": "每日调仓时计算: cushion = 当前净值 - (历史最高净值 × (1 - cppi_floor)), exposure = min(1.0, cppi_multiplier × cushion). 满仓时exposure=1.0, 亏损时自动减仓",
    "example_full": "净值1.0, floor净值=0.95 → cushion=0.05 → exposure=min(1,20×0.05)=1.0 → 满仓",
    "example_drawdown": "净值0.97, floor净值=0.95 → cushion=0.02 → exposure=min(1,20×0.02)=0.40 → 40%仓位",
    "example_deep_dd": "净值0.96, floor净值=0.95 → cushion=0.01 → exposure=0.20 → 20%仓位"
  },

  "performance_v52": {
    "_method": "WF OOS无泄露评估 + CPPI(5,20)",
    "v52_weighted": "74.0% A+",
    "L1_signal": "81%",
    "L2_efficiency": "70%",
    "L3_risk_control": "55%",
    "L5_excess": "93%",
    "L9_conditional_robustness": "78%"
  },

  "evolution": {
    "ng1.0.0": "62特征, 绝对标签, V5.2=71.5%",
    "ng1.0.1": "69特征, 行业超额标签, V5.2=71.5%",
    "ng1.0.2": "69特征 + downside模型 + CPPI(5,20), V5.2=74.0%"
  },

  "prev_production": {
    "version": "v4.9.0.1",
    "v52_score": "54.1% B级 (WF-OOS)"
  },

  "cli_commands": {
    "daily_cache_update": "python3 ml_models/ng/ng_cache_updater.py --date {DATE} --version ng1.0.2",
    "daily_selection": "python3 backtest/batch_generate_v395_reports.py --version ng1.0.2 --start-date {DATE} --end-date {DATE} --output-dir reports/daily_selection_ng102",
    "backtest_eval": "python3 backtest/run_north_star_eval.py --backtest --report-dir reports/daily_selection_ng102 --label NG102-PROD --top-n 10 --focus-days 10 --rank-field composite --cppi-floor 0.05 --cppi-multiplier 20"
  }
}
